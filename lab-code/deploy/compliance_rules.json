{
  "_comment": "Compliance gate: every control must pass before release.",
  "required_controls": [
    {"id": "no_hardcoded_secrets", "desc": "No secrets in source", "blocking": true},
    {"id": "dockerfile_nonroot",   "desc": "Container runs as non-root USER", "blocking": true},
    {"id": "dockerfile_pinned_base","desc": "Base image is pinned (no :latest)", "blocking": true},
    {"id": "no_high_cves",         "desc": "No HIGH-severity dependency CVEs", "blocking": true},
    {"id": "artifact_signed",      "desc": "Release artifact is signed", "blocking": true}
  ]
}
