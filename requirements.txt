# AI Security for Developers and Practitioners (Full Day)
#
# Python dependencies:
#   * pyyaml    - Lab 8 policy engine reads a YAML policy file.
#   * chromadb  - Lab 2 uses a real local Chroma vector database with real
#                 embeddings (Chroma's built-in all-MiniLM-L6-v2, downloaded
#                 once during setup). chromadb uses onnxruntime, not torch,
#                 so it stays CPU-light.
# Everything else uses the Python standard library, including the LLM helper
# (common/llm.py), which talks to Ollama and Groq over plain HTTP.
#
# The language model itself is NOT a pip package:
#   * Ollama (llama3.2:3b) is installed and the model pulled by
#     scripts/startup_ollama.sh during Codespace creation (the default).
#   * Groq (llama-3.1-8b-instant / llama-3.3-70b-versatile) is a hosted API --
#     set GROQ_API_KEY to use it for all five model labs (faster, free tier).

pyyaml>=6.0
chromadb>=0.5

# Lab 5: real MCP server + scoped JWT auth
fastmcp>=2.0
pyjwt>=2.8

# Lab 6: real OpenTelemetry instrumentation
opentelemetry-sdk>=1.20
